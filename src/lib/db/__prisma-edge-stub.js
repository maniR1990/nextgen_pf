// Empty stub — Prisma requires Node.js native binaries and cannot run in Edge.
// Prisma usage in instrumentation.ts is guarded by NEXT_RUNTIME !== 'nodejs',
// so this stub is never actually executed at runtime.
module.exports = {};
